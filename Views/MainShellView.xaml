<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="PeopleCodeIDECompanion.Views.MainShellView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <NavigationView x:Name="AppNavigationView"
                        IsBackButtonVisible="Collapsed"
                        IsSettingsVisible="False"
                        PaneDisplayMode="Left"
                        SelectionChanged="AppNavigationView_SelectionChanged">
            <NavigationView.MenuItems>
                <NavigationViewItem x:Name="OracleConnectionNavigationItem"
                                    Content="Oracle Connection"
                                    Tag="OracleConnection">
                    <NavigationViewItem.Icon>
                        <SymbolIcon Symbol="World" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem x:Name="PeopleCodeInterfaceNavigationItem"
                                    Content="PeopleCode Interface"
                                    Tag="PeopleCodeInterface">
                    <NavigationViewItem.Icon>
                        <SymbolIcon Symbol="Document" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem x:Name="ReferenceExplorerNavigationItem"
                                    Content="Reference Explorer"
                                    Tag="ReferenceExplorer">
                    <NavigationViewItem.Icon>
                        <SymbolIcon Symbol="Library" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.MenuItems>

            <ContentControl x:Name="ContentHost" />
        </NavigationView>

        <Border x:Name="ExpandedObjectStatusBorder"
                Width="296"
                Margin="12,68,0,12"
                Padding="12"
                HorizontalAlignment="Left"
                VerticalAlignment="Bottom"
                Background="{ThemeResource ControlFillColorSecondaryBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="8">
            <StackPanel Spacing="10">
                <TextBlock Text="Object Status"
                           Style="{ThemeResource BodyStrongTextBlockStyle}" />
                <StackPanel x:Name="ObjectStatusPanel"
                            Spacing="6" />
            </StackPanel>
        </Border>

        <Border x:Name="CompactObjectStatusBorder"
                Margin="4,68,0,12"
                Padding="4"
                HorizontalAlignment="Left"
                VerticalAlignment="Bottom"
                Background="{ThemeResource ControlFillColorSecondaryBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="18"
                Visibility="Collapsed">
            <StackPanel x:Name="CompactObjectStatusPanel"
                        Spacing="4" />
        </Border>
    </Grid>
</UserControl>
