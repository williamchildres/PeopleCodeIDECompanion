<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="PeopleCodeIDECompanion.Views.PeopleCodeInterfaceView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    HorizontalAlignment="Stretch"
    VerticalAlignment="Stretch"
    MinWidth="1480"
    MinHeight="920"
    mc:Ignorable="d">

    <Grid Padding="20,16,20,12"
          RowSpacing="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid ColumnSpacing="16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Grid RowSpacing="3"
                  HorizontalAlignment="Left"
                  MaxWidth="760">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock Text="PeopleCode Interface"
                           Style="{ThemeResource SubtitleTextBlockStyle}" />

                <TextBlock x:Name="HeaderContextSummaryTextBlock"
                           Grid.Row="1"
                           MaxLines="1"
                           TextTrimming="CharacterEllipsis"
                           TextWrapping="NoWrap"
                           Style="{ThemeResource CaptionTextBlockStyle}"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
            </Grid>

            <StackPanel Grid.Column="1"
                        Spacing="6"
                        HorizontalAlignment="Right">
                <Grid ColumnSpacing="12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Spacing="4">
                        <TextBlock Text="Profile"
                                   Style="{ThemeResource CaptionTextBlockStyle}"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        <ComboBox x:Name="ProfileComboBox"
                                  Width="200"
                                  DisplayMemberPath="DisplayName"
                                  SelectionChanged="ProfileComboBox_SelectionChanged" />
                    </StackPanel>

                    <StackPanel Grid.Column="1"
                                Spacing="4">
                        <TextBlock Text="Object type"
                                   Style="{ThemeResource CaptionTextBlockStyle}"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        <ComboBox x:Name="ObjectTypeComboBox"
                                  Width="168"
                                  SelectionChanged="ObjectTypeComboBox_SelectionChanged">
                            <x:String>All Objects</x:String>
                            <x:String>App Package</x:String>
                            <x:String>App Engine</x:String>
                            <x:String>Record</x:String>
                            <x:String>Page</x:String>
                            <x:String>Component</x:String>
                        </ComboBox>
                    </StackPanel>

                    <StackPanel Grid.Column="2"
                                Spacing="4">
                        <TextBlock Text="Refresh"
                                   Style="{ThemeResource CaptionTextBlockStyle}"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        <Button x:Name="RefreshModeButton"
                                Click="RefreshModeButton_Click"
                                ToolTipService.ToolTip="Reload the current PeopleCode object type">
                            <FontIcon Glyph="&#xE72C;" />
                        </Button>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </Grid>

        <InfoBar Grid.Row="1"
                 x:Name="HeaderInfoBar"
                 IsOpen="False"
                 IsClosable="True"
                 Severity="Warning" />

        <ScrollViewer Grid.Row="2"
                      x:Name="ModeContentScrollViewer"
                      Margin="0,2,0,0"
                      HorizontalScrollBarVisibility="Auto"
                      VerticalScrollBarVisibility="Auto"
                      SizeChanged="ModeContentScrollViewer_SizeChanged">
            <Grid x:Name="ModeContentViewport"
                  HorizontalAlignment="Stretch"
                  VerticalAlignment="Stretch">
                <ContentControl x:Name="ModeContentHost"
                                HorizontalAlignment="Stretch"
                                VerticalAlignment="Stretch"
                                HorizontalContentAlignment="Stretch"
                                VerticalContentAlignment="Stretch" />
            </Grid>
        </ScrollViewer>

        <Border Grid.Row="3"
                Margin="0,10,0,0"
                Padding="0,8,0,0"
                BorderThickness="0,1,0,0"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
            <ScrollViewer HorizontalScrollMode="Enabled"
                          HorizontalScrollBarVisibility="Hidden"
                          VerticalScrollMode="Disabled"
                          VerticalScrollBarVisibility="Disabled"
                          ZoomMode="Disabled">
                <StackPanel x:Name="ObjectStatusBarPanel"
                            Orientation="Horizontal"
                            Spacing="8"
                            HorizontalAlignment="Right" />
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>
