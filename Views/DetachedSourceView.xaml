<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="PeopleCodeIDECompanion.Views.DetachedSourceView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="20"
          RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Background="{ThemeResource ControlFillColorSecondaryBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="8"
                Padding="16">
            <StackPanel Spacing="8">
                <Grid ColumnSpacing="12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Spacing="4">
                        <TextBlock x:Name="ObjectTypeTextBlock"
                                   Style="{ThemeResource CaptionTextBlockStyle}"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        <TextBlock x:Name="ObjectTitleTextBlock"
                                   Style="{ThemeResource TitleTextBlockStyle}"
                                   TextWrapping="WrapWholeWords" />
                        <TextBlock x:Name="ObjectSubtitleTextBlock"
                                   TextWrapping="WrapWholeWords" />
                    </StackPanel>

                    <TextBlock Grid.Column="1"
                               VerticalAlignment="Top"
                               Style="{ThemeResource BodyStrongTextBlockStyle}"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                               Text="Read-only" />
                </Grid>

                <Grid ColumnSpacing="8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <Button x:Name="SaveButton"
                            Content="Save"
                            IsEnabled="False" />

                    <Button x:Name="SaveCompileButton"
                            Grid.Column="1"
                            Content="Save + Compile"
                            IsEnabled="False" />

                    <Button x:Name="RevertButton"
                            Grid.Column="2"
                            Content="Revert"
                            IsEnabled="False" />

                    <TextBlock x:Name="AuthoringSummaryTextBlock"
                               Grid.Column="3"
                               VerticalAlignment="Center"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                               TextWrapping="WrapWholeWords" />
                </Grid>

                <TextBlock x:Name="ProfileContextTextBlock"
                           TextWrapping="WrapWholeWords"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}" />

                <TextBlock x:Name="LastUpdatedTextBlock"
                           TextWrapping="WrapWholeWords"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}" />

                <TextBlock x:Name="MetadataSummaryTextBlock"
                           TextWrapping="WrapWholeWords" />

                <TextBlock x:Name="AuthoringDetailTextBlock"
                           TextWrapping="WrapWholeWords"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
            </StackPanel>
        </Border>

        <Border Grid.Row="1"
                Background="{ThemeResource ControlFillColorSecondaryBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="8"
                Padding="16">
            <Grid RowSpacing="8">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Grid ColumnSpacing="8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="PeopleCode Source"
                               Style="{ThemeResource SubtitleTextBlockStyle}" />

                    <Button x:Name="CopySourceHeaderButton"
                               Grid.Column="1"
                               Content="Copy"
                               Click="CopySourceButton_Click" />

                    <TextBlock x:Name="SourceMatchStatusTextBlock"
                               Grid.Column="2"
                               VerticalAlignment="Center"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}" />

                    <Button x:Name="PreviousSourceMatchButton"
                            Grid.Column="3"
                            Content="Previous Match"
                            IsEnabled="False"
                            Click="PreviousSourceMatchButton_Click" />

                    <Button x:Name="NextSourceMatchButton"
                            Grid.Column="4"
                            Content="Next Match"
                            IsEnabled="False"
                            Click="NextSourceMatchButton_Click" />
                </Grid>

                <ScrollViewer x:Name="SourceScrollViewer"
                              Grid.Row="1"
                              HorizontalScrollBarVisibility="Auto"
                              VerticalScrollBarVisibility="Auto">
                    <RichTextBlock x:Name="SourceRichTextBlock"
                                   MinHeight="420"
                                   FontFamily="Consolas"
                                   IsTextSelectionEnabled="True"
                                   TextWrapping="NoWrap" />
                </ScrollViewer>
            </Grid>
        </Border>
    </Grid>
</UserControl>
